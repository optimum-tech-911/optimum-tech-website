import React from "react";
import { motion } from "framer-motion";

export const ProjectCard = ({ title, desc, href }) => (
  <motion.a
    href={href}
    target="_blank"
    rel="noopener noreferrer"
    className="group block rounded-xl border border-gray-700/60 p-4 bg-secondary/60 hover:shadow-glow transition-shadow"
    initial={{ opacity: 0, scale: 0.95 }}
    whileInView={{ opacity: 1, scale: 1 }}
    viewport={{ once: true, amount: 0.3 }}
    whileHover={{ y: -4 }}
  >
    <div className="aspect-video rounded-lg bg-[#0f1520] border border-gray-700/60 mb-4 group-hover:border-primary/60 transition-colors" />
    <div className="flex items-start justify-between gap-4">
      <div>
        <h3 className="text-lg font-semibold text-white">{title}</h3>
        <p className="text-sm text-gray-400 mt-1">{desc}</p>
      </div>
      <span className="text-primary opacity-0 group-hover:opacity-100 transition-opacity text-sm">Visit â†’</span>
    </div>
  </motion.a>
);

